# Claude Code internal
.claude/

# Node
node_modules/

# API Keys
.env

# User account data (개인 계정 데이터 — GitHub 업로드 금지)
data/user/roster.json
data/user/settings.json
data/user/history.json

# Temp / test files
*.tmp
stage6_test.mjs

# Import pipeline scratch space (downloaded source data, not project data)
tools/import/.cache/

# Google OAuth client secret 파일 (절대 커밋 금지)
client_secret_*.json
*.apps.googleusercontent.com.json

# 관리자 작업 대기열 (로컬 전용)
data/pending-tasks.json
